apiVersion: apps/v1
kind: Deployment
metadata:
  name: vote-redis
  namespace: ambassador
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vote-redis
  template:
    metadata:
      labels:
        app: vote-redis
    spec:
      containers:
        - name: vote-redis
          image: redis
---
apiVersion: v1
kind: Service
metadata:
  name: vote-redis-srv
  namespace: ambassador
spec:
  selector:
    app: vote-redis
  ports:
    - name: db
      protocol: TCP
      port: 6379
      targetPort: 6379
